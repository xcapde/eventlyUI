import { DetailText } from "../../styles/styles.styled"

export const ParticipationXS = ({participation}) => {
    
    return (
        <DetailText>
                {participation} joined
        </DetailText>            
    )
}