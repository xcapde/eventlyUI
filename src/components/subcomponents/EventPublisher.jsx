import { DetailText } from "../../styles/styles.styled"

export const EventPublisher = ({publisher}) => {
    return (
        <DetailText>
            {publisher} Publisher
        </DetailText>
    )
}